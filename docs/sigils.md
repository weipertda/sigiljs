# Sigils

> A sigil is a type expression that describes the structure of data, what data should look like, and what 'types' should be manifested. SigilJS also lets you write schema expressions that validate runtime data.

Sigils are 'blueprints' for data.

Example:

```javascript
Sigil`string`
```

This sigil matches any string.
